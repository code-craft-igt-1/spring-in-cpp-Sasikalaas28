#include "stats.h"

Stats Statistics::ComputeStatistics(const std::vector<double>& in) {
    //Implement statistics here
    int len = in.size();
    int sum = 0.0;
    Stats st;
    st.min = in[0];
    st.max = in[0];
    st.
    for(int i=0;i<len;i++)
        {
            
            
        }
}
